# Development-only auth bypass configuration.
# Active ONLY while running `npm run dev` (Vite development mode).
# Production builds ignore these entirely - normal login is always required.
VITE_DEV_AUTOLOGIN=off
VITE_DEV_GYM_ID=MAIN
VITE_DEV_LOGIN_EMAIL=admin@gmail.com
VITE_DEV_LOGIN_PASSWORD=admin2026
VITE_DEV_LOGIN_ROLE=admin
